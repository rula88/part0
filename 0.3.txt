user-> browser:
note left of browser
user writes the adress in the adress bar
https://fullstack-exampleapp.herokuapp.com/notes
end note

browser->server: GET https://fullstack-exampleapp.herokuapp.com/main.js
server->browser: status 304, main.js

browser->server: GET https://fullstack-exampleapp.herokuapp.com/data.json
server->browser: status 304, data.json

browser->server: GET https://fullstack-exampleapp.herokuapp.com/notes
note left of server
  HTML is generated in the existing ones
  the number of notes updated
end note
server->browser: status 304, page HTML code

note left of browser
 the browser displays the HTML returned by the server.
 it displays list generated by the javascript in the
 returned javascript file and fill it with the data in the
 returned json file
end note
